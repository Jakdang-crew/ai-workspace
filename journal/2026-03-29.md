# 2026-03-29 (일)

## 오늘 한 일

- AI native 성장을 위한 워크스페이스 구축
- 4가지 영역으로 구조 설계
  - `notes/` - 학습 노트 및 개념 정리
  - `practices/` - AI 도구 활용 실습 코드
  - `projects/` - 프로젝트 실험 및 프로토타입 (Git submodule)
  - `journal/` - 일지/회고록
- Git 저장소 초기화 및 GitHub 연결
- SSH 키 생성 및 GitHub 등록

## 배운 것: Git Submodule

### 왜 사용하는가?
- 메인 워크스페이스와 각 프로젝트가 독립적인 git history를 가짐
- 전체 워크스페이스 상태를 특정 시점으로 복원 가능

### 새 프로젝트 추가 방법

**방법 1: GitHub에서 시작 (권장)**
```bash
# GitHub에서 저장소 생성 후
git submodule add git@github.com:user/project.git projects/project-name
```

**방법 2: 로컬에서 시작**
```bash
# 1. 프로젝트 생성 및 초기화
cd projects && mkdir my-project && cd my-project && git init

# 2. 작업 후 GitHub에 빈 저장소 생성 (README 없이)

# 3. 원격 연결 및 푸시
git remote add origin git@github.com:user/my-project.git
git push -u origin main

# 4. submodule 등록 (폴더 이동 방식 - 간단)
cd ~/ai-workspace
mv projects/my-project ../backup
git submodule add git@github.com:user/my-project.git projects/my-project
rm -rf ../backup
```

### 일상적인 작업 흐름
```bash
# 프로젝트에서 작업 후
cd projects/my-project
git add . && git commit -m "작업 내용" && git push

# 메인 워크스페이스에서 참조 업데이트
cd ~/ai-workspace
git add projects/my-project
git commit -m "Update my-project submodule"
git push
```

### 클론할 때
```bash
git clone --recursive <repo-url>
# 또는
git clone <repo-url> && git submodule update --init --recursive
```

### AI 네이티브 백엔드 개발자 로드맵 작성

- 6~12개월 3단계 성장 전략 수립
  - 1단계: 워크플로우 안정화
  - 2단계: Kotlin + Spring Boot + AI (Spring AI, RAG, AWS Bedrock)
  - 3단계: 실전 포지셔닝 (포트폴리오, 기술 블로그)
- 마일스톤 기반 액션 트래커 작성 (`notes/action-tracker.md`)
- 로드맵 문서화 (`notes/ai-native-roadmap.md`)

### Claude Code Plugin Marketplace 구축

- GitHub 저장소 생성: `htcmw/claude-marketplace`
- 워크스페이스에 submodule로 등록: `projects/claude-marketplace`
- 플러그인 표준 스펙 정의 (`docs/plugin-spec.md`)
  - 카테고리: agents, skills, hooks, rules, bundles
  - 매니페스트: `.claude-plugin/plugin.json`
- ADR-001: Plugin Format 작성
- CONTRIBUTING.md 작성

### 유틸리티 스크립트 작성

- `scripts/pull-all.sh` — 전체 submodule + 메인 rebase pull
- `scripts/push-all.sh` — 전체 submodule push + 참조 업데이트 (동적 커밋 메시지)

## 배운 것

### Claude Code 플러그인 시스템

- **Skills**: `.claude/skills/<name>/SKILL.md` — YAML frontmatter + Markdown, 슬래시 커맨드로 호출
- **Agents**: `.claude/agents/<name>.md` — 커스텀 서브에이전트 정의, 도구/모델 제한 가능
- **Hooks**: `settings.json`의 `hooks` 키 — 이벤트 기반 자동화 (PreToolUse, PostToolUse 등)
- **Rules**: `.claude/rules/*.md` — 경로 기반 조건부 로딩, 코딩 규칙 적용
- **Plugins**: `.claude-plugin/plugin.json` 매니페스트로 위 모든 것을 하나의 모듈로 패키징

## 느낀 점

AI와 함께 성장하기 위한 첫 발걸음을 내딛었다. 기록을 꾸준히 남기는 것이 중요하다. Git submodule은 처음엔 복잡해 보이지만, 프로젝트별 독립 관리가 필요할 때 유용하다.

Claude Code의 확장 시스템이 생각보다 체계적이다. 플러그인 포맷이 공식으로 정의되어 있어서 마켓플레이스를 만들기 좋은 구조다. 도구를 모듈화해서 관리하면 워크플로우 재사용성이 크게 올라갈 것 같다.

## 다음 할 일

- [ ] CLAUDE.md 설계 (역할 정의, 컨벤션, 금지 패턴)
- [ ] 첫 번째 플러그인 작성 → 마켓플레이스에 등록
- [ ] 워크플로우 구축 및 일관성 검증
